            in=i;
            max=prefix[i];
        {
        for(int i=l-1;i>=0;i--)
            if(max>1)
                break;
        int max=prefix[l-1],in=l-1;
        //     return prefix[l-1];
        
        }
        if(in==l-1)
            return max;
        System.out.println("Max : "+max+" in : "+in);
