        if(prefix[l-1]>prefix[l-2])
            return prefix[l-1];
        int max=prefix[0],in=0;
        for(int i=0;i<l;i++)
        {
            if(prefix[i]>max&&prefix[i]>1){
        }
                max=prefix[i];
                in=i;
            }
        for(int i=in;i<l;i++)
        int sum=max;
        }
        {
            sum+=s[i];
        }
        return max-sum;
    }
}
